
from django.contrib import admin
from django.urls import path
from . import views
import debito.views

urlpatterns = [
    #path('gastos/', views.gastosHTML, name='gastos'),
    #path('percentualUsado/', views.percentualUsado, name='percentualUsado'),
    #path('creditos/', views.creditosHTML, name='creditos'),
    #path('alldebitsDF', views.alldebitsDF, name='alldebitsDF'),
]
